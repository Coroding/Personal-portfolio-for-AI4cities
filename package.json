{
  "name": "mingqi-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve": "python -m http.server 8765 --bind 127.0.0.1",
    "capture:previews": "node scripts/capture-previews.js"
  },
  "devDependencies": {
    "playwright": "^1.55.0"
  }
}
